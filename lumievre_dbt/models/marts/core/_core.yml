version: 2

models:
  - name: customer_360
    columns:
      - name: customer_id
        tests: [not_null, unique]

  - name: churn_features
    columns:
      - name: customer_id
        tests: [not_null, unique]
      - name: monetary
        tests: [not_null]
      - name: churned
        tests: [not_null]